# 长期记忆

## 当前学习主题
- PostgreSQL 数据库基础与实践

## 技术背景摘要
- PostgreSQL（简称 PG）是当今功能最强大的开源关系型数据库系统，以其稳定性、容错性、丰富的高级特性及可扩展性著称，广泛应用于企业核心业务。

## 工具选型结果及原因
- 本地直接安装。原因：检测到你的电脑尚未安装 Docker，为了避免引入额外的学习成本，直接在 Windows 本地安装是最快且最省心的方式。
- 学习路径：A1（应用开发主线）。原因：与学习者现有 Python/Django 背景衔接最快，优先保障业务落地能力。

## 路线图概览
- ✅ 第 1 步：环境搭建 —— 已完成 PostgreSQL 运行、`psql` 连接与最小 SQL 连通性验证。
- ⬜ 第 2 步：表设计 —— 完成 users/projects/tasks 三表设计及数据类型选型。
- ⬜ 第 3 步：基础 SQL —— 掌握 CRUD、排序、分页、聚合与多表 Join。
- ⬜ 第 4 步：约束与性能基础 —— 掌握主键/外键/唯一/非空约束与索引基础。
- ⬜ 第 5 步：事务与 Python 集成 —— 完成事务提交/回滚与 `psycopg` 最小脚本接入。

## 验收标准（2 周）
- 环境验收：可启动 PostgreSQL，且可通过 `psql` 与图形工具连接同一数据库。
- 建模验收：完成 users/projects/tasks 三张核心表，并正确使用主键、外键、非空、唯一约束。
- SQL 能力验收：可独立完成 CRUD，且可写至少 5 条典型多表查询（Join、分组、排序、分页）。
- 事务与索引验收：完成 1 次事务回滚演示，并为高频查询添加索引并说明前后差异。
- Python 集成验收：使用 `psycopg` 完成连接、插入、查询、提交/回滚最小示例。
- 交付件验收：提供 `schema.sql`、`queries.sql`、1 个 Python 最小示例脚本及简短复盘。

## 关键决策记录
- 初始学习主题确定为 PostgreSQL。
- 学习路径已确认：A1（应用开发主线）。
- 验收标准由导师定义并获学生确认（2 周目标）。

## session-archive
- [2026-02-28 18:13:11] 第 2 步：表设计 —— 解释阶段（users/projects/tasks 三表目标与关系）。
- [2026-02-28 18:13:11] 错误：`Get-Command psql -ErrorAction SilentlyContinue` 无输出，表示 `psql` 不在当前 PATH。
- [2026-02-28 18:13:11] 解决方案：定位 `psql.exe` 于 `C:\Program Files\PostgreSQL\15\bin\psql.exe`，验证 `psql --version` 成功，并完成用户级 PATH 写入后重开终端验证通过。
- [2026-02-28 18:13:11] 已完成阶段 0（全局概览）：主题确认为 PostgreSQL，学习路径确认 A1（应用开发主线）。
